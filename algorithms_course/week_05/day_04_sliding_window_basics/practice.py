# Неделя 5, День 4 - Sliding Window: база
#
# Задание 1:
# Реализуй window_sums(items, k).
#
# Задание 2:
# Реализуй max_sum_window(items, k).
#
# Задание 3:
# Реализуй count_windows_above_threshold(items, k, threshold).


def window_sums(items: list[int], k: int) -> list[int]:
    return []



def max_sum_window(items: list[int], k: int) -> int:
    return -1



def count_windows_above_threshold(items: list[int], k: int, threshold: int) -> int:
    return -1


if __name__ == "__main__":
    sample = [2, 1, 5, 1, 3, 2]
    print(window_sums(sample, 3))
    print(max_sum_window(sample, 3))
    print(count_windows_above_threshold(sample, 3, 8))
    print("Реализуй задачи на sliding window.")
