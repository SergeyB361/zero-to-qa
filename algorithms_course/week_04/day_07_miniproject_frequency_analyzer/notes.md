# Неделя 4, День 7 - Мини-проект: Frequency Analyzer

## Цель дня
Собрать небольшой анализатор данных, который использует `dict`, `set` и frequency counting.

## Идея проекта
Ты получаешь список элементов и строишь по нему:
- frequency map;
- количество уникальных элементов;
- самый частый элемент;
- элементы, встретившиеся ровно один раз.

## Что важно
- не делать лишних проходов там, где можно переиспользовать frequency map;
- разделить код на маленькие функции;
- сделать понятный demo flow.

## Следующий шаг
Открой `spec.md` и реализуй проект в `frequency_analyzer.py`.
