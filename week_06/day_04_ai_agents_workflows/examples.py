AGENT_TASKS = [
    "Проверить, какие мини-проекты в репозитории незавершены, и перечислить только конкретные файлы с причинами.",
    "Подготовить тесты для одной функции и не менять остальные модули.",
    "Сделать обзор структуры проекта и отдельно перечислить риски без правок в коде.",
]


def show_agent_tasks() -> None:
    for task in AGENT_TASKS:
        print(task)


if __name__ == "__main__":
    show_agent_tasks()
