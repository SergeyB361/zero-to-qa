# Неделя 11, День 5 — Сложность операций set

## Главное
Проверка membership в `set` обычно работает быстро: в среднем `O(1)`.

## Почему так
Под капотом используется хеш-таблица.

## Практика дня
Сравни на уровне идеи, почему `x in set` часто быстрее, чем `x in list`.
