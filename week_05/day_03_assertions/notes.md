# Неделя 5, День 3 — Assertions

## Что такое assertion
`assert` проверяет, что условие истинно. Если нет, тест падает.

## Что обычно проверяют
- равенство значений
- вхождение элемента
- тип результата
- текст ошибки

## Проверка исключений
Для ожидаемых ошибок часто используют `pytest.raises`.

```python
import pytest

def test_divide_by_zero():
    with pytest.raises(ZeroDivisionError):
        10 / 0
```

## Практика дня
Открой `practice.py` и подготовь функции, для которых удобно писать разные типы проверок.
