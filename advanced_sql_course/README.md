# Курс: SQL — продвинутый уровень

Pure advanced SQL-трек для аналитических запросов, оптимизации, транзакций и сложных отчётов.

## Зачем нужен курс
- перейти от базового SQL к зрелому запросному мышлению;
- разбираться в аналитических функциях, производительности и консистентности;
- изучать продвинутый SQL без смешения с Python- и QA-интеграцией.

## Формат
- `4` недели
- `7` дней в неделе
- обычный день: `notes.md`, `examples.sql`, `practice.sql`
- день мини-проекта: `notes.md`, `spec.md`, основной `.sql` файл

## Как выполнять SQL без Python
- рекомендуемый вариант на Windows: `DB Browser for SQLite`, `DBeaver` или `SQLiteStudio`;
- открой `.sql` файл целиком и выполняй его по секциям сверху вниз;
- сначала выполняй `examples.sql`, затем `practice.sql`;
- в мини-проектах выполняй основной `.sql`-скрипт после чтения `spec.md`;
- если установлен `sqlite3` CLI, можно запускать и через него, но курс на это не завязан.

## Структура курса
- `week_01/` — Advanced querying
- `week_02/` — Optimization and indexing
- `week_03/` — Transactions and consistency
- `week_04/` — Advanced SQL reporting

## Связанные файлы
- `ROADMAP.md` — полный план по неделям и дням
