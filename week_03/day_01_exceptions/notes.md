# Неделя 3, День 1 — Исключения

## Зачем нужны исключения
Исключения позволяют корректно обрабатывать ошибки во время выполнения программы, а не падать с traceback.

## Базовая конструкция
```python
try:
    # код, где может быть ошибка
except ValueError:
    # что делать при этой ошибке
else:
    # выполнится, если ошибки не было
finally:
    # выполнится всегда
```

## Частые типы ошибок
- `ValueError` — неверное значение (например, `int("abc")`)
- `ZeroDivisionError` — деление на ноль
- `FileNotFoundError` — файл не найден
- `KeyError` — ключа нет в словаре

## Практические правила
- Лови конкретные ошибки (`except ValueError`, а не просто `except`).
- Не скрывай ошибку молча: выводи понятное сообщение.
- `finally` используй для действий, которые должны произойти всегда.
- Если данные некорректны по бизнес-правилу, можно бросить ошибку вручную через `raise`.

## Мини-пример
```python
value = "10"

try:
    number = int(value)
    result = 100 / number
except ValueError:
    print("Нужно ввести целое число")
except ZeroDivisionError:
    print("Делить на ноль нельзя")
else:
    print("Результат:", result)
finally:
    print("Проверка завершена")
```

## Практика
Открой `practice.py`, решай по шагам.

